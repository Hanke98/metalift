#INFO: { "loopvars": ["k", "j"], "inputs": [["depth", "int"], ["vol_flux_x", "double[_N*_N]"],["vol_flux_x_out", "double[_N*_N]"], ["x_max", "int"], ["x_min", "int"], ["y_max", "int"], ["y_min", "int"]] }

k = (y_min - depth)
while {k, (k < (y_max + depth))}
{
j = 1
while {j, (j < depth)}
{
vol_flux_x_out[(((x_max + j) + 1) + (((x_max + 3) - (x_min - 2)) * k))] = -vol_flux_x[(((x_max + 1) - j) + (((x_max + 3) - (x_min - 2)) * k))]
j = (j + 1)
}


k = (k + 1)
}

